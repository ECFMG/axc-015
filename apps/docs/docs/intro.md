---
slug: /
---

# agentCourses API

agentCourses is a dark software factory scaffold. HTTP capabilities are composed by `@apps/api` and served through Hono routes in `@axc/rest`. The healthcheck reports process liveness. `GET /api/courses` searches the training course catalog.
