---
slug: /
---

# agentCourses API

agentCourses is a dark software factory scaffold. The first HTTP capability is the healthcheck, composed by `@apps/api` and served through the Hono route in `@axc/rest`.
